# Configuration shared by `dart test` and `flutter test`.
# VM-only suites live outside the default `test/` directory, so we no longer
# need to exclude them globally. Additional presets can be added here later.
